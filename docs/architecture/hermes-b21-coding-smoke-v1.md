# Hermes B21 coding smoke v1

Later B24 update: [recovery supplement v1](hermes-b24-recovery-supplement-v1.md)
bridges the legacy identity ordering and records later verification separately.
Original B21 REFUSED/acceptance_failed/spent remain unchanged; missing historical
fixture ownership keeps recovery BLOCKED. No cleanup or execution grant follows.
The outcomes and successor proposals below retain their historical scope.


Follow-up: [B22 read-only diagnosis](hermes-b22-footprint-diagnosis-v1.md)
now records an independent test PASS, unchanged extras and two recovery blockers.
B21 remains acceptance_failed/spent; the original REFUSED result and historical
next-step statements below are preserved, not replaced by the later test.

RF-RUNTIME-005B21, 2026-09-16. ADR-004 v13 authorizes exactly one new isolated
coding smoke after B20 recovery, with no whole-task retry, resume or fallback.
This does not replay B13/B14/B17 or reactivate B20's spent recovery exception.

## Fixed authorization and preparation

The scope is `one_real_hermes_coding_smoke_b21_only`, using the unchanged exact
Hermes 0.21.2 pin, `openai-codex`, `gpt-5.6-sol`, reasoning `medium` and only
`file,terminal`. A new owned root contains one fresh local arithmetic repository:
`add(a,b)` subtracts; the existing Node test expects addition. Baseline must fail
with exit 1. Acceptance permits only the exact minimal implementation repair.
There is no runtime process/port authority, local commit, push or deployment.
An edit-only application lease and one global Writer reserve the attempt.

The sealed task restricts work to the assigned root and necessary files, requires
the existing `node --test`, and excludes credentials/user files, network tools,
dependency installation, other repositories/worktrees/temporary roots/application
instances, destructive Git, commits, pushes and Git configuration changes. Only
the authenticated model transport is authorized. Existing native-shell network
and outside-root observation limitations remain the accepted v7 residual risk;
task restrictions are not presented as OS isolation or a network firewall.

The B21 activation is opaque, process-local, single-use and bound to fresh task,
attempt, workspace, Ready, policy, executable, model/reasoning, tools and deadline.
Limits remain one attempt, at most 300 seconds with a 5-second cleanup margin,
24 logical turns and API retry setting 2. No API/config/JSON approval, old scope,
process restart or model response can mint another grant. A separate durable B21
spent record preserves B13/B14/B17 and the complete B20 recovery journal.

Preflight checks B20's chained terminal evidence, old pair/barrier absence and
unchanged spent artifacts. Canonical physical installation paths are used; the
Windows application-data alias is not accepted as an identity. Fresh split
inventory verification checks every immutable/generated file, profile v5 and
disabled lazy installs without a provider status/help/loader command or reading
credential contents. Only a real Worker-owned native Job can start the provider.

## Durable review and cleanup

[B19's durable order](hermes-root-scoped-review-reconciliation-v2.md) applies:
Job completion, post-footprint, private bounded signed evidence, independent test
and exact diff/status verification while the fixture exists, split installation
readback, terminal receipt, owned cleanup, application lease release, Writer
release. Generated-cache reconciliation, if necessary, uses only the existing
B16 source/interpreter/header/body verification and transactional publication;
immutable/package/source/launcher drift never receives an exception.

An unknown safe in-root file is an acceptance/scope failure, not a security
violation. If its presence prevents safe execution of the fixed test, the
receipt retains that refusal and the original fixture; cleanup is not authorized.
Protected path/Git/root/application drift remains a boundary violation. Copies
of receipts do not supply cleanup authority. Missing or negative durable evidence
cannot produce API review approval.

Exit zero is only a candidate. Smoke DONE requires `verified_candidate`, an
independent existing-test PASS, exact allowed minimal diff, installation PASS,
genuine stopped Job and successful owned cleanup/lease/Writer release. It is not
task completion or release authority: those public receipt flags remain closed.
Unobservable model/tool/token/cost counters remain null. Raw prompt and provider
output are never published or stored as repository evidence.

## Result and stop

**BLOCKED after exactly one authorized provider start.** The provider exited 0,
but durable review returned `acceptance_failed`, `scopeReviewRequired=true`, with
9 content-path changes and 0 protected changes/violations. The expected
implementation change is accompanied by 5 additional directories and 3 files.
Their identities/digests/categories are retained privately; withheld relative
labels and public HMAC identifiers do not establish who created them or why.

Independent verification refused with `smoke_unexpected_diff` before running
the existing test. A subsequent read-only exact-byte comparison confirms the
expected implementation repair and unchanged original test. It is not a test
PASS and does not satisfy single-file acceptance. No second provider start or
post-refusal execution of potentially expanded test discovery was performed.

| Observation | Result |
| --- | --- |
| Usage precheck | 59% used, ordinary usage allowed, below 75% stop threshold |
| Policy / activation / start | Qualified; explicitly authorized; exactly one start |
| Model / reasoning | Requested exact gpt-5.6-sol / medium; response identity not observable in quiet |
| Baseline | Existing Node test FAIL, exit 1 |
| Provider | Exit 0; candidate only; provider phase 36.442 seconds |
| Controller total | 134.072 seconds |
| Independent post-test | REFUSED before execution; smoke_unexpected_diff |
| Read-only implementation/test comparison | Exact minimal implementation bytes and unchanged existing test |
| Durable evidence | Signed final review read back matching report digest; snapshot, metadata and reason preserved |
| Installation before/after | PASS, 23,653 immutable + 826 generated files; 0 changed/added/removed; no receipt update |
| Native Job | Assigned before resume; root_exit; cleanup true; active processes 0 |
| Fixture cleanup | Denied by genuine terminal review; original owned fixture retained |
| Writer/application lease | Retained for reconciliation; original controller absent |
| Final bounded process check | No owner child, managed executable or Job launcher observed |
| Old state | B13/B14/B17 spent bytes/identities preserved; B20 journal complete and exception still spent |
| B21 authorization | New spent record retained; grant revoked/nonreusable; activationAuthorized=false |
| Model/tool/transport/token/cost counts | null; not inferred from prose or wall time |

No recovery barrier was introduced after B21. The fixture, Writer and one
edit-only application lease are intentionally retained, not claimed as completed
cleanup. The stored process/identity chain is not a cleanup grant when independent
verification is refused. B19's guard is preserved; no B20-style exception is used.

Validation passed before the real attempt: 355 regressions across 17 runtime
test files, 25 focused B19/B21 cases, 8 API review/provider cases, and application
lint/typecheck/build. The optional installed Python compiler test was excluded.
Synthetic owned fixtures/processes finished; the only intentional live-attempt
leftovers are the retained B21 review artifacts above. No model output was used
as evidence of acceptance.

**One next owner action:** authorize RF-RUNTIME-005B22 as a source/read-only
diagnosis of the eight additional B21 entries and a concrete safe recovery plan
for its retained fixture/lease/Writer. This does not authorize deletion, another
model run or a B21 retry. No second B21 start is allowed; this atom stops after
its local commit and coordinator report.

All six public flags remain false; registry/profile v5 and native-risk reference
v7 are unchanged. No production, Docker/WSL, database, MCP, login, push or deploy
operation belongs to this atom.
